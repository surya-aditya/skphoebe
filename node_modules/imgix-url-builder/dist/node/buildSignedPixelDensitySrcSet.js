import { buildSignedURL } from "./buildSignedURL.js";
const buildSignedPixelDensitySrcSet = (url, secureURLToken, { pixelDensities, ...params }) => {
  return pixelDensities.map((dpr) => {
    return `${buildSignedURL(url, secureURLToken, {
      ...params,
      dpr
    })} ${dpr}x`;
  }).join(", ");
};
export {
  buildSignedPixelDensitySrcSet
};
//# sourceMappingURL=buildSignedPixelDensitySrcSet.js.map
