import { buildSignedURL } from "./buildSignedURL.js";
const buildSignedWidthSrcSet = (url, secureURLToken, { widths, ...params }) => {
  return widths.map((width) => {
    return `${buildSignedURL(url, secureURLToken, {
      ...params,
      w: void 0,
      width
    })} ${width}w`;
  }).join(", ");
};
export {
  buildSignedWidthSrcSet
};
//# sourceMappingURL=buildSignedWidthSrcSet.js.map
