import { buildURL } from "../buildURL.js";
import { signURL } from "./signURL.js";
const buildSignedURL = (url, secureURLToken, params) => {
  return signURL(buildURL(url, params), secureURLToken);
};
export {
  buildSignedURL
};
//# sourceMappingURL=buildSignedURL.js.map
