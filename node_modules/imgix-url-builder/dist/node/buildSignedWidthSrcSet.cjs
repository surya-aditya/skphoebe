"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const buildSignedURL = require("./buildSignedURL.cjs");
const buildSignedWidthSrcSet = (url, secureURLToken, { widths, ...params }) => {
  return widths.map((width) => {
    return `${buildSignedURL.buildSignedURL(url, secureURLToken, {
      ...params,
      w: void 0,
      width
    })} ${width}w`;
  }).join(", ");
};
exports.buildSignedWidthSrcSet = buildSignedWidthSrcSet;
//# sourceMappingURL=buildSignedWidthSrcSet.cjs.map
