"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const buildSignedURL = require("./buildSignedURL.cjs");
const buildSignedPixelDensitySrcSet = (url, secureURLToken, { pixelDensities, ...params }) => {
  return pixelDensities.map((dpr) => {
    return `${buildSignedURL.buildSignedURL(url, secureURLToken, {
      ...params,
      dpr
    })} ${dpr}x`;
  }).join(", ");
};
exports.buildSignedPixelDensitySrcSet = buildSignedPixelDensitySrcSet;
//# sourceMappingURL=buildSignedPixelDensitySrcSet.cjs.map
